language: cpp
dist: trusty
sudo: false
matrix:
  include:
    - os: linux
      compiler: gcc-4.8
      env: GENERATOR=Makefiles
      addons:
        apt:
          sources:
            - ubuntu-toolchain-r-test
            - deadsnakes
            - kubuntu-backports
          packages:
            - g++-4.8
            - python3.5
            - python3.5-dev
            - python3.5-venv
            - libglu1-mesa-dev
            - libxxf86vm-dev
            - libxrandr-dev
            - libxinerama-dev
            - libxcursor-dev
            - libxi-dev
            - libx11-dev
            - cmake
      script:
        - cmake --version
        - cmake -DNANOGUI_USE_GLAD=ON -DNANOGUI_PYTHON_VERSION=3.5 -DPYTHON_INCLUDE_DIRS:PATH=/usr/include/python3.5m -DPYTHON_LIBRARY:FILEPATH=/usr/lib/x86_64-linux-gnu/libpython3.5m.so -DCMAKE_CXX_COMPILER=g++-4.8 -DCMAKE_C_COMPILER=gcc-4.8
        - make -j 2
    - os: linux
      compiler: clang
      env: GENERATOR=Ninja
      addons:
        apt:
          sources:
            - deadsnakes
            - kubuntu-backports
          packages:
            - python3.5
            - python3.5-dev
            - python3.5-venv
            - libglu1-mesa-dev
            - libxxf86vm-dev
            - libxrandr-dev
            - libxinerama-dev
            - libxcursor-dev
            - libxi-dev
            - libx11-dev
            - cmake
            - ninja-build
      before_install:
        # NOTE: by requesting compiler: clang, this is installed by default.
        #       Travis CI does not correctly setup CC / CXX, as well as G++
        #       headers will get used.  These paths may become stale in the
        #       future, in which case this must be updated.
        - export CXX="/usr/local/clang-5.0.0/bin/clang++"
        - export CC="/usr/local/clang-5.0.0/bin/clang"
        - export CPATH="/usr/local/clang-5.0.0/include/c++/v1"
        - export LD_LIBRARY_PATH="/usr/local/clang-5.0.0/lib"
      script:
        - cmake --version
        - cmake -DNANOGUI_USE_GLAD=ON -DNANOGUI_PYTHON_VERSION=3.5 -DPYTHON_INCLUDE_DIRS:PATH=/usr/include/python3.5m -DPYTHON_LIBRARY:FILEPATH=/usr/lib/x86_64-linux-gnu/libpython3.5m.so
        - make -j 2
    - os: osx
      compiler: clang
      env: GENERATOR=Makefiles
      script:
        - cmake --version
        - cmake -DNANOGUI_PYTHON_VERSION=2.7
        - make -j 2
    - os: osx
      compiler: clang
      env: GENERATOR=Ninja
      before_install:
        # See: https://docs.travis-ci.com/user/reference/osx/#A-note-on-upgrading-packages
        - brew outdated cmake || brew upgrade cmake
        - brew install ninja
      script:
        - cmake --version
        - ninja --version
        - cmake -G Ninja -DNANOGUI_PYTHON_VERSION=2.7
        - ninja
    - os: linux
      language: docs
      env: STYLE
      script:
      - resources/check-style.sh
